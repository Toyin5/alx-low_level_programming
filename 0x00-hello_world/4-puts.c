#include <stdio.h>
#include <string.h>
/*
 * A function that uses puts
 *
 */
int main(void)
{
    char str[55];
    strcpy(str, "\"Programming is like building a multilingual puzzle");
    puts(str);
    return 0;
}

