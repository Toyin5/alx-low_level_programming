#include <stdio.h>
#include <string.h>
/**
 * main - output a text using puts
 * Return: 0 if success
 *
 */
int main(void)
{
	char str[55];
	strcpy(str, " \"Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}
